#include "main.h"

/**
 * _strlen- get the length of a string
 * @s: signifies string
 * Return: length of string
 */

int _strlen(char *s)
{
	char *a;
	int y;

	a = "Joy is lovely";
	y = _strlen(str);
	
	{
	
		printf("%d\n, y");
		y++
	}
	return (y);
}
