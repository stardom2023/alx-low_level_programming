#include "main.h"

/**
 *main - entry point
 *description : "multiply 2 numbers"
 *Return: 1 for a character that will be a digit or 0 for anything else
 */

int mul(int x, int y)
{
	return (x *y);
}
