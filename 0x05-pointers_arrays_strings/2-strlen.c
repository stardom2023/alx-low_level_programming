#include "main.h"

/**
 * _strlen- get the length of a string
 * @s: signifies string
 * Return: length of string
 */

int _strlen(char *s)
{
	char s;
	int len;

	s = "Joy is lovely";
	len = _strlen;

	while (s = '\0')
	{
	len++;
	s++;
	}
	return (len);
}
