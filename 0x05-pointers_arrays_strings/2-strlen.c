#include "main.h"

/**
 * _strlen- get the length of a string
 * @s: signifies string
 * Return: length of string
 */

int _strlen(char *s)
{
	char *s;
	int lent;

	s = "Joy is lovely";
	lent = _strlen(str);

	while (*s = '\0')
	{
	lent++;
	s++;
	}
	return (y);
}
